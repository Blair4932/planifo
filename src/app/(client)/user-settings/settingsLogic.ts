import { SettingTab } from "./settingsTabs";
import { MouseEventHandler } from "react";
import * as settingInfo from "./page";

export const handleTabSelect = (
  tab: SettingTab
): MouseEventHandler<HTMLLIElement> => {
  return;
};

export const fetchUser = async () => {};
